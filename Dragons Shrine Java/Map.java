class Map{
//Field 
String name;
String imageName;
String pathName;
MapTile mapTiles[];
String filename;

//Constructor

    public void map(String name, String imageName, String pathName){
        name = null;
        imageName = null;
        pathName = null;
    }


//Methods
    public void loadMapFile(String pathName){
        MapTile mapTiles[];
    }

    public char getTileFeatures(int x, int y)
    {

    if(){}
    return char
    }

    public boolean[] checkFeature(char letter, int x, int y)
    {
          
        if(tileString[i].contains = "s"){
            mapTile[row][column].STATUE = true;
        }
        if(tileString[i].contains = "d"){
            mapTile[row][column].DIFFICULT = true;
        }
        if(tileString[i].contains = "c"){
            mapTile[row][column].SACRED_CIRCLE = true;
        }
        if(tileString[i].contains = "u"){
            mapTile[row][column].SUMMONING_CIRCLE = true;
        }
        if(tileString[i].contains = "^"){
            mapTile[row][column].SPIKE_STONES = true;
        }
        if(tileString[i].contains = "r"){
            mapTile[row][column].RISKY = true;
        }
        if(tileString[i].contains = "6"){
            mapTile[row][column].BLOOD_ROCK = true;
        }
        
        
        if(tileString[i].contains = "w"){
            mapTile[row][column].Solid = true;
        }

    } //end check Feature
}//end class Map

